import Orientation from 'react-native-orientation';

const App = () => {
  //   useEffect(() => {
  //     Orientation.lockToLandscapeLeft(); // 첫 시작할때 방향 설정
  //     Orientation.addOrientationListener(onOrientationChange);//리스너 설정
  //     return () => {
  //     	Orientation.unlockAllOrientations(),
  //     	Orientation.removeOrientationListner(onOrientationChange)
  //     }
  //     ,[]}
  // const onOrientationChange = (orientation) => {   // 세로가 되면 다시 강제로 가로 모드
  //     if (orientation === 'PORTRAIT') {
  //         Orientation.lockToLandscapeLeft();
  //     }
  // }
  // return (...)
};
